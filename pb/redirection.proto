syntax = "proto3";

service Redirection {
    rpc ImageHandle (Image) returns (ImageResponse) {}
//    rpc ImageHandleStream(stream Image) returns (ImageResponse) {}
}

message Image {
    bytes image = 1;
    string message = 2;
}

message ImageResponse {}
